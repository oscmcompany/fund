apiVersion: 1

datasources:
  - name: Fly Prometheus
    type: prometheus
    access: proxy
    url: https://api.fly.io/prometheus/personal/
    isDefault: true
    jsonData:
      httpHeaderName1: Authorization
    secureJsonData:
      httpHeaderValue1: ${FLY_PROMETHEUS_TOKEN}
